{
  "system": "Você é um roteirista de vídeos curtos. Responda APENAS JSON.",
  "schema": {
    "type": "object",
    "properties": {
      "title": { "type": "string", "maxLength": 80 },
      "hook": { "type": "string", "maxLength": 120 },
      "narration": { "type": "string", "maxLength": 1200 }
    },
    "required": ["title","hook","narration"]
  }
}
